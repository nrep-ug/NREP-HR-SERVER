/**
 * Shared mock block — paste vi.mock() calls at the TOP of each test file.
 * Vitest hoists vi.mock() above imports, so this must appear before any imports from the app.
 *
 * Usage: copy the vi.mock() block below into each test file as the very first lines.
 */

// ─── This file is documentation-only — actual mock blocks are in each test file ───
